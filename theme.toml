[mgr]
marker_symbol = "|"
border_symbol = "|"

[tabs]
sep_inner = { open = "<", close = ">" }
sep_outer = { open = "{", close = "}" }

[indicator]
padding = { open = "[", close = "]" }

[status]
sep_left = { open = "", close = "" }
sep_right = { open = "", close = "" }

[which]
separator       = "==="

[notify]
icon_info  = "i"
icon_warn  = "!"
icon_error = "?"

[cmp]
icon_file    = " "
icon_folder  = "/"
icon_command = "*"

[icon]
globs = []
dirs  = []
files = []
exts  = []
conds = []
